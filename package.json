{
  "name": "wedding-invitation",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "check": "node --check api/wedding.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": "22.x"
  }
}
